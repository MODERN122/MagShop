<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CloudMarket.Views.ItemsPage"
             Title="{Binding Title}"
             x:Name="BrowseItemsPage">

    <ContentPage.ToolbarItems>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <Grid RowDefinitions="40,40,*">
            <StackLayout Orientation="Horizontal">
                <Picker SelectedIndex="{Binding ItemSelectedIndex, Mode=TwoWay}" ItemsSource="{Binding Categories}" ItemDisplayBinding="{Binding Name}" 
                        WidthRequest="150" Title="Выбор категории"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal"  Grid.Row="1">
                <StackLayout Orientation="Horizontal" BindableLayout.ItemsSource="{Binding FilteredProperties}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <Frame CornerRadius="10" BackgroundColor="Red">
                                <Label Text="{Binding PropertyName}" WidthRequest="80"/>
                            </Frame>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </StackLayout>
                    <Grid>
                        
                        <Label Text="+"/>
                <Picker ItemsSource="{Binding Properties}" ItemDisplayBinding="{Binding PropertyName}" Opacity="0" WidthRequest="100"/>
                    </Grid>
                    <!--<Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding AddPropertyFilterCommand}"/>
                    </Frame.GestureRecognizers>-->
            </StackLayout>
            <RefreshView IsRefreshing="{Binding IsBusy, Mode=TwoWay}" Command="{Binding LoadItemsCommand}"
                         Grid.Row="2">
                <CollectionView x:Name="ItemsCollectionView"
                ItemsSource="{Binding Items}" Margin="10">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" Span="2" VerticalItemSpacing="10" HorizontalItemSpacing="10"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame CornerRadius="10" HasShadow="True">
                                <Grid RowDefinitions="Auto,20,20, 20" ColumnDefinitions="Auto">
                                    <Image Source="test_product" Grid.Row="0"/>
                                    <Label Text="{Binding ProductName}"  Grid.Row="1"/>
                                    <Label Text="{Binding PriceNew}"  Grid.Row="2"/>
                                    <Label Text="{Binding Rating}" Grid.Row="3"/>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>
        </Grid>
    </ContentPage.Content>
</ContentPage>